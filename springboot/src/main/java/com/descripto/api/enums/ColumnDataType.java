package com.descripto.api.enums;

/**
 * Enum representing the data types for Excel columns
 * 
 * @author Descripto Team
 */
public enum ColumnDataType {
    STRING,     // Text data
    NUMBER,     // Numeric data (integers, decimals)
    BOOLEAN     // Boolean values (true/false, yes/no)
}
